<IfModule mod_rewrite.c>
    RewriteEngine On

    # Kalau akses bukan ke folder public, redirect ke public
    RewriteCond %{REQUEST_URI} !^/public
    RewriteRule ^(.*)$ public/$1 [L,QSA]

    RewriteRule ^storage/(.*)$ ../storage/app/public/$1 [L]

</IfModule>
